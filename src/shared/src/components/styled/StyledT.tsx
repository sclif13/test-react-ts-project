import { T } from '@admiral-ds/react-ui'
import styled from 'styled-components'
import { spacer, SpacingAttr } from '../../helpers/spacer'

export const StyledT = styled(T) <SpacingAttr>`
  ${spacer};
`;